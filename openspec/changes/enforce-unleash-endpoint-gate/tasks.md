## 1. Baseline and feasibility gates (prove the premises before editing)

- [ ] 1.1 Record the defect being closed, from a run rather than from reading: with no endpoint started, `JAVA_HOME=/home/wantez/.jdks/azul-17.0.20.1 ./mvnw -B -pl spring-boot-starter-openfeature-unleash test` must show `Skipped: 1` for `UnleashEndpointIntegrationTest` **and** `BUILD SUCCESS`. Verify both appear in the same log — that pairing is the whole problem, and if it does not reproduce, stop before changing anything.
- [ ] 1.2 Confirm the BOM-priced Testcontainers release actually loads on the pinned JDK 17 **before** adopting it, since `2.x` compatibility is unverified: resolve it with `JAVA_HOME=/home/wantez/.jdks/azul-17.0.20.1 ./mvnw -B -pl spring-boot-starter-openfeature-unleash dependency:list`, then compile a throwaway reference and confirm it is executed, not merely resolvable. If the managed release carries a class-file floor above Java 17, pin the managed `testcontainers.version` property down to the newest release that loads on 17 — read the class-file majors across **every** class of the candidate jars rather than a single proxy, as `upgrade-dependencies-2026-09` task 2.2 had to — and **never** raise `java.version` to make it pass.

## 2. Dependencies (priced by the BOM, no new version literal)

- [ ] 2.1 Add test-scope `org.testcontainers:testcontainers`, `org.testcontainers:junit-jupiter`, `org.testcontainers:testcontainers-postgresql` to `spring-boot-starter-openfeature-unleash/pom.xml` **unversioned**, and verify by reading them back from `dependency:list` output rather than from a green build. The module is `…-postgresql`: `org.testcontainers:testcontainers-postgres` returns `404` from Central, so the intuitive name resolves only as a failure.
- [ ] 2.2 Verify no version literal was introduced: `grep -nE 'testcontainers' spring-boot-starter-openfeature-unleash/pom.xml pom.xml` shows coordinates without `<version>` in the module and no new `testcontainers` property in the root, and the versions read back in 2.1 equal the Spring Boot-managed `testcontainers-bom` values.

## 3. Self-provisioning gate (spec: "The endpoint gate provisions its own dependencies reproducibly")

- [ ] 3.1 Provision the endpoint from inside the test — Postgres plus the Unleash server — with an explicit wait on the server's own health endpoint and a deliberately generous deadline, replacing the `Assumptions.assumeTrue(isEndpointUp(), …)` pre-check. Verify a clean run with nothing started beforehand reports `Tests run: 1, Failures: 0, Errors: 0, `**`Skipped: 0`**.
- [ ] 3.2 Pin each image by digest, obtaining the value with `docker image inspect --format '{{index .RepoDigests 0}}' <image>` rather than transcribing it, and verify the pin by re-reading the pinned image's OCI label `org.opencontainers.image.version` and asserting it reports `8.2.0` — the version this gate's evidence was produced against. Verify the assertion is executed (fails when deliberately pointed at another digest), so a moved mutable tag cannot silently change what the gate runs.
- [ ] 3.3 Make credential provisioning fail loudly instead of quietly: start from fresh endpoint state each run and assert the admin credential is **accepted by the server** before any flag is authored. Verify by pre-seeding the token table so seeding would be skipped — the run must fail with a cause naming the credential, not proceed to a vacuous pass.
- [ ] 3.4 Keep authoring honest where the server is misleading: create the flag, then call the per-environment enable/disable endpoint for the environment the client token is scoped to, because the admin create endpoint ignores the `environments` block it is handed. Verify the full round trip — authored-enabled evaluates true, authored-disabled false, absent false — and that the enabled case cannot be satisfied by a fallback, since every evaluation falls back to false.

## 4. Enforcement: prove it can fail (spec: "The build executes the real-endpoint provider gate")

- [ ] 4.1 Prove the gate is discriminating rather than vacuous: invert one expected outcome (author the enabled flag as disabled) and verify the module run reports `Failures: 1` naming that flag, then restore and verify green. Do not mark this done from a green run alone.
- [ ] 4.2 Prove absence of a container runtime is a **failure** rather than a skip: run the module with the container endpoint pointed at a non-existent daemon and verify `BUILD FAILURE` plus a `Skipped: 0` line for the class. A `Skipped: 1` here means the guard was softened and the change has not achieved its purpose.
- [ ] 4.3 Prove an endpoint that never becomes healthy is attributable: point the gate at an image reference that cannot start, and verify the reported failure names the endpoint as the cause, includes the endpoint's own log output, and is textually distinguishable from a red assertion — infrastructure must never masquerade as a regression.

## 5. CI reachability and diagnosability

- [ ] 5.1 Verify the gate is enforced without a functional workflow edit by running the PR job's own command verbatim on the canonical toolchain — `JAVA_HOME=/home/wantez/.jdks/azul-17.0.20.1 ./mvnw -B --no-transfer-progress clean verify` — and verify the gate class reports `Skipped: 0` inside that run with all 17 modules `SUCCESS`. Confirm `.github/workflows/pull_request.yml` still starts nothing: enforcement must come from the test being unconditionally executable, not from new CI wiring.
- [ ] 5.2 Make a red PR attributable without a local Docker reproduccion: ensure endpoint/container log output reaches the build log and is uploaded on failure. Verify by inducing the 4.3 failure and confirming the artifact or log contains the endpoint's lines, not only a stack trace.
- [ ] 5.3 Record the new contributor requirement where contributors will meet it: the module now needs a working container runtime, and the only bypass is an explicit, visible skip rather than a default. Verify the text is present in `CONTRIBUTING.md` (or the module note) and states plainly that a Docker-less build of this module fails by design.

## 6. Full gate and disposition of the manual route

- [ ] 6.1 Run the full gate `JAVA_HOME=/home/wantez/.jdks/azul-17.0.20.1 ./mvnw -B -Psonar clean verify` and verify `BUILD SUCCESS`, all 17 modules `SUCCESS`, no module `SKIPPED`, 0 Checkstyle violations, every coverage check met, and the gate class present with `Skipped: 0` — verified by grepping the log for `SKIPPED`/`FAILURE [` and finding none rather than by reading the `BUILD SUCCESS` line alone. If the new test shifts module coverage, re-measure the floor from the module's JaCoCo CSV after a clean run and round down to the nearest 0.01 before this check.
- [ ] 6.2 Decide and record the disposition of `src/test/resources/unleash/docker-compose.integration.yml`, which is no longer the only route to the gate: either keep it as the manual/diagnostic path with its header stating so, or generate it from the same pinned references the test uses so the two cannot drift. Verify the chosen state is reflected in the file header and that the manual route still brings up an endpoint the gate can use, if retained.
