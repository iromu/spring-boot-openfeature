server.port: 9998
spring:
  openfeature:
    unleash:
      app-name: ${spring.application.name}
      environment: development
  application:
    name: UnleashApplication

logging.level.org.iromu: DEBUG
